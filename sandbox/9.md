---
layout: default
title: "Sandbox9"
sitemap: false
robots: noindex
---

This sandbox page is created to test authors order in publication category. This includes all pages which have more than one author under the "Publications" category, and fetches yaml data parameters. Publications include books, book chapters, and papers.

## List of pages

{% assign pub_pages = site.pages | where_exp: "item", "item.categories contains 'Publications'" %}
{% assign multi_author_pages = pub_pages | where_exp: "item", "item.authors.size > 1" %}
{% if multi_author_pages.size > 0 %}
<ul>
  {% for item in multi_author_pages %}
  <li>
    <a href="https://sunilabraham.in{{ item.permalink }}">{{ item.title }}</a><br>
    <strong>Authors:</strong> {{ item.authors | join: ", " }}<br>
    <strong>Date:</strong> {{ item.date | date: "%d %B %Y" }}<br>
    <strong>Created:</strong> {{ item.created }}
  </li>
  {% endfor %}
</ul>
{% else %}
<p>No multi-author publications found.</p>
{% endif %}
